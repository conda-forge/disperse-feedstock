diff --git a/CMakeLists.txt b/CMakeLists.txt
index b47d1e7..7f11462 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -50,16 +50,16 @@ SET(CMAKE_INSTALL_PREFIX "${PROJECT_SOURCE_DIR}" CACHE PATH "DISPERSE install pr
 )
 ENDIF(CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT)
 
-CMAKE_MINIMUM_REQUIRED(VERSION 2.6.3)
+CMAKE_MINIMUM_REQUIRED(VERSION 3.6)
 SET(CMAKE_ALLOW_LOOSE_LOOP_CONSTRUCTS  true)
 LIST(APPEND CMAKE_MODULE_PATH "${PROJECT_SOURCE_DIR}/modules")
-if("${CMAKE_MAJOR_VERSION}.${CMAKE_MINOR_VERSION}" VERSION_GREATER 2.6.3)
-  if("${CMAKE_MAJOR_VERSION}.${CMAKE_MINOR_VERSION}.${CMAKE_PATCH_VERSION}" VERSION_GREATER 2.8.3)
-    cmake_policy(VERSION 2.8.4)
-  else()
-    cmake_policy(VERSION 2.6.3)
-  endif()
-endif()
+# if("${CMAKE_MAJOR_VERSION}.${CMAKE_MINOR_VERSION}" VERSION_GREATER 2.6.3)
+#   if("${CMAKE_MAJOR_VERSION}.${CMAKE_MINOR_VERSION}.${CMAKE_PATCH_VERSION}" VERSION_GREATER 2.8.3)
+#     cmake_policy(VERSION 2.8.4)
+#   else()
+#     cmake_policy(VERSION 2.6.3)
+#   endif()
+# endif()
 
 SET( QT_USE_QTMAIN   TRUE )
 #SET( QT_USE_QTOPENGL   TRUE ) #not needed
